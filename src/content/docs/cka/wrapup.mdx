---
title: "마무리"
description: "지금까지 본 것을 하나로"
deckGroup: exam-strategy
sidebar:
  order: 210
legacyThesis: true
---

import { Steps, Card, CardGrid, Tabs, TabItem, LinkCard } from '@astrojs/starlight/components';

> 지금까지 본 것을 하나로

## 전체 지도

```d2 layout=elk pad=40 title="선언된 상태 spec 이 API 서버 · 컨트롤러 · 스케줄러 · kubelet 을 거쳐 실제 상태 status 가 되고 다시 차이를 좁히는 조정 루프, 그리고 Service · 스토리지 · RBAC · NetworkPolicy 가 붙는 자리"
direction: down
classes: {
  ok: { style: { fill: "#dcfce7"; stroke: "#16a34a"; font-color: "#14532d" } }
  bad: { style: { fill: "#fee2e2"; stroke: "#dc2626"; font-color: "#7f1d1d" } }
  warn: { style: { fill: "#fef3c7"; stroke: "#d97706"; font-color: "#78350f" } }
  key: { style: { fill: "#dbeafe"; stroke: "#2563eb"; font-color: "#1e3a8a" } }
  mute: { style: { fill: "#f1f5f9"; stroke: "#94a3b8"; font-color: "#334155" } }
  zone: { style: { fill: "#ede9fe"; stroke: "#7c3aed"; font-color: "#4c1d95" } }
}
a: 선언된 상태 · spec { class: mute }
b: API 서버\n인증 → 인가 →\nadmission → etcd { class: key }
c: 컨트롤러들\nDeployment · ReplicaSet\n· Job … { class: mute }
d: 스케줄러\nnodeName 결정 { class: mute }
e: kubelet\n컨테이너 실행 · probe { class: mute }
f: 실제 상태 · status { class: ok }
g: Service · DNS ·\nIngress { class: warn }
h: PV/PVC · CSI { class: warn }
i: RBAC { class: warn }
j: NetworkPolicy —\nCNI가 집행 { class: warn }

a -> b
b -> c
c -> d
d -> e
e -> f
f -> c: 차이를 좁힌다 { style.stroke-dash: 3 }
g -> e
h -> e
i -> b
j -> e
```

**[시작하기 전에](/cka/intro/)에서 말한 한 문장으로 돌아온다** — 모든 것은 **spec과 status의 차이를 줄이는 루프**다.

## 개념별로 남길 한 줄

| 개념 | 한 줄 |
|---|---|
| 아키텍처 | 모든 화살표는 **API 서버**를 향한다. 컨트롤 플레인은 **스태틱 Pod** |
| kubectl | **명령형으로 만들고 `$do`로 뽑아 고친다** |
| Pod | **liveness는 죽이고 readiness는 트래픽만 끊는다** |
| 워크로드 | Deployment → ReplicaSet → Pod. **옛 RS가 롤백의 재료** |
| 설정 | **requests는 스케줄링, limits는 런타임 강제** |
| 스케줄링 | **affinity는 Pod이 고르고, taint는 노드가 밀어낸다** |
| 오토스케일링 | **metrics-server + requests**가 없으면 HPA는 아무것도 못 한다 |
| Service | 진단의 1번은 **`get endpointslices`** |
| DNS | **`nslookup kubernetes.default`** 가 기준점 |
| Ingress/Gateway | 규칙은 리소스, 일은 **컨트롤러**가 한다 |
| NetworkPolicy | **정책이 붙는 순간 기본 거부.** egress면 **DNS를 열어라** |
| 스토리지 | **RWO는 노드 하나.** `Multi-Attach`는 여기서 온다 |
| [API 요청](/cka/api-access/) | 인증 → 인가 → Admission 순서로 실패 지점을 나눈다. |
| [사용자 인증](/cka/user-authentication/) | CSR 제출 → 승인 → 인증서 추출 → kubeconfig 등록. |
| [ServiceAccount](/cka/service-account/) | Pod의 신원과 작업 권한은 별개다. 토큰은 수명이 있고 kubelet이 갱신한다. |
| [RBAC](/cka/rbac/) | Role은 권한, Binding은 연결. 검산은 `auth can-i --as`. |
| [Admission](/cka/admission/) | 작업 권한을 통과한 요청의 내용을 고치거나 검증한다. |
| [클러스터 설치](/cka/cluster-setup/) | 런타임 준비 → init → CNI 설치 → join. |
| [노드 유지보수](/cka/node-maintenance/) | 워크로드를 비우고 정비한 뒤 노드와 Pod 복귀를 확인한다. |
| [업그레이드](/cka/cluster-upgrade/) | 첫 컨트롤 플레인은 `apply`, 나머지는 `node`. |
| [인증서 관리](/cka/certificates/) | 만료 확인 → 갱신 → 사용하는 컴포넌트에 반영. |
| [etcd 백업과 복구](/cka/etcd-backup/) | 스냅샷을 새 data-dir에 복구하고 etcd가 그 경로를 읽게 한다. |
| [HA](/cka/high-availability/) | 컨트롤 플레인을 다중화하고 etcd 과반수를 유지한다. |
| [Helm](/cka/helm/) | 차트와 값을 조합하고 릴리스 이력을 관리한다. |
| [Kustomize](/cka/kustomize/) | 공통 base에 환경별 overlay를 겹친다. |
| [확장 인터페이스](/cka/extensions/) | CRI는 런타임, CNI는 네트워크, CSI는 스토리지 연결이다. |
| [CRD](/cka/crd/) | API 종류를 만들지만 선언을 실행하는 컨트롤러는 따로 필요하다. |
| [Operator](/cka/operator/) | 사용자 정의 리소스의 spec과 실제 상태 차이를 줄인다. |
| 트러블슈팅 | **고치기 전에 층을 확정하라** |

## 반복해야 하는 것 — 딱 다섯 가지

이해로 끝나지 않고 **손이 기억해야** 하는 것들이다.

<CardGrid>
<Card title="1 · 클러스터 업그레이드" icon="rocket">
저장소 URL → `apply`/`node` → kubelet → `uncordon`.
**killercoda**에서 5회 이상.
</Card>
<Card title="2 · etcd 백업·복구" icon="database">
인증서 3종, 새 data-dir, `etcd.yaml` 수정.
**killercoda**에서 5회 이상.
</Card>
<Card title="3 · 고장난 컨트롤 플레인 복구" icon="warning">
`crictl` → 로그 → 매니페스트 수정.
**killercoda**에서 5회 이상.
</Card>
<Card title="4 · RBAC 만들고 검산" icon="padlock">
Role → Binding → `auth can-i --as`.
**kind**에서 연습 가능.
</Card>
<Card title="5 · 네트워크 진단 5단계" icon="random">
localhost → Pod IP → ClusterIP → DNS → 외부.
**kind**에서 연습 가능.
</Card>
</CardGrid>

각각 **5회 이상** 반복하면 시험장에서 생각하지 않고 손이 움직인다.

## 자주 되돌아올 페이지

시험 직전에는 이 페이지들만 훑어도 충분하다.

```d2 layout=elk pad=40 title="시험 직전에 볼 자리 — 각 장의 마지막 요약 절과 시험 전략 치트시트 · 트러블슈팅 진단 지도 · 클러스터 운영 업그레이드 순서 · API 접근과 권한 Role 조합 · 스케줄링 스케줄링 메시지 · Service 통신 판별"
direction: right
classes: {
  ok: { style: { fill: "#dcfce7"; stroke: "#16a34a"; font-color: "#14532d" } }
  bad: { style: { fill: "#fee2e2"; stroke: "#dc2626"; font-color: "#7f1d1d" } }
  warn: { style: { fill: "#fef3c7"; stroke: "#d97706"; font-color: "#78350f" } }
  key: { style: { fill: "#dbeafe"; stroke: "#2563eb"; font-color: "#1e3a8a" } }
  mute: { style: { fill: "#f1f5f9"; stroke: "#94a3b8"; font-color: "#334155" } }
  zone: { style: { fill: "#ede9fe"; stroke: "#7c3aed"; font-color: "#4c1d95" } }
}
d: 시험 직전 { class: key }
a: 각 장의 마지막 요약 절 { class: mute }
b: 시험 전략 치트시트\n생성 · 조회 · 진단 · 운영 · etcd { class: mute }
c: 트러블슈팅 상태별 진단 지도\nSTATUS 를 보면 다음 명령이 정해진다 { class: mute }
e: 클러스터 운영 업그레이드 순서 요약 { class: mute }
f: API 접근과 권한 Role/Binding 조합 { class: mute }
g: 스케줄링 스케줄링 실패 메시지 { class: mute }
h: Service 어디서 끊겼는지 판별하기 { class: mute }

d -> a
d -> b
d -> c
d -> e
d -> f
d -> g
d -> h
```

사이드바에서 각 장으로 바로 갈 수 있고, 상단 검색(⌘K)으로 키워드를 찾아도 된다.

## 시험 이후 — 실무로 가는 길

CKA는 **관리자 관점**의 기초다. 그 다음은 관심사에 따라 갈린다.

| 방향 | 다음 |
|---|---|
| **보안** | CKS(Certified Kubernetes Security Specialist) — PSA(Pod Security Admission, [스케줄링](/cka/scheduling/)), admission webhook, 런타임 보안, 이미지 스캔 |
| **개발** | CKAD(Certified Kubernetes Application Developer) — 워크로드 설계 관점 (범위가 겹치니 비교적 쉽다) |
| **플랫폼** | GitOps(ArgoCD), Helm 차트 작성, 오퍼레이터 개발 |
| **관측성** | Prometheus, Grafana, OpenTelemetry, 로그 파이프라인 |
| **클라우드** | EKS/GKE 운영 — IRSA(IAM Roles for Service Accounts), 노드그룹, Karpenter, VPC CNI |

**다만 순서를 잘못 잡지 말 것.** 이 덱의 클러스터 운영과 트러블슈팅이 흔들리면
어떤 방향으로 가도 결국 같은 자리로 돌아오게 된다.

## 마지막으로

CKA는 **암기 시험이 아니라 손 시험**이다.\
이 덱을 세 번 읽는 것보다\
**업그레이드를 세 번 직접 하는 것**이 낫다.

```d2 layout=elk pad=40 title="막혔을 때 개념 · 실습 · 클러스터 이상 세 증상별로 돌아갈 곳"
direction: right
classes: {
  ok: { style: { fill: "#dcfce7"; stroke: "#16a34a"; font-color: "#14532d" } }
  bad: { style: { fill: "#fee2e2"; stroke: "#dc2626"; font-color: "#7f1d1d" } }
  warn: { style: { fill: "#fef3c7"; stroke: "#d97706"; font-color: "#78350f" } }
  key: { style: { fill: "#dbeafe"; stroke: "#2563eb"; font-color: "#1e3a8a" } }
  mute: { style: { fill: "#f1f5f9"; stroke: "#94a3b8"; font-color: "#334155" } }
  zone: { style: { fill: "#ede9fe"; stroke: "#7c3aed"; font-color: "#4c1d95" } }
}
q: 막혔다 { shape: diamond; class: mute }
a: 이 덱으로 돌아온다 { class: mute }
b: 클러스터를 띄운다 { class: ok }
c: describe 의 Events 를 읽는다\n대부분 답이 거기 있다 { class: key }

q -> a: 개념이 흔들린다
q -> b: 손이 안 움직인다
q -> c: 클러스터가 이상하다
```

합격을 빈다. 🎉

<LinkCard
  title="시험 전략과 치트시트"
  description="시험 직전에 다시 볼 곳. 생성·조회·진단·운영·etcd 치트시트가 모여 있다."
  href="/cka/exam-strategy/"
/>
