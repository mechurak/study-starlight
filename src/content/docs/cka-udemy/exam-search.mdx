---
title: "작업별 명령·문서 검색 색인"
description: "시험 작업을 첫 관찰 명령과 공식 문서 검색어, 해당 CKA 실습 페이지로 연결하는 짧은 색인"
reviewedAt: 2026-09-13
---

import { LinkCard } from '@astrojs/starlight/components';
import Thesis from '../../../components/docs/Thesis.astro';

<Thesis>
문제의 명사를 리소스로, 동사를 첫 명령으로 바꾼 뒤 이 표의 영문 검색어로 공식 문서 예제를 찾고,
연결된 실습 페이지의 완료 기준으로 결과를 판정한다.
</Thesis>

이 페이지는 정답 모음이 아니라 **출발점 색인**이다. 먼저 context·namespace·대상 노드를 확인하고,
표의 첫 명령으로 현재 상태를 관찰한다. 작성법이 필요하면 공식 문서에서 표시된 검색어를 찾고,
링크된 실습에서 바꿀 필드와 실제 성공 조건을 확인한다.

시험 브라우저에서는 허용된 공식 사이트 내부 검색을 사용한다. 설치·업그레이드 문서는 문제의
Kubernetes 버전으로 바꾸고, KodeKloud·GitHub 예제와 학습 보충은 Quick Reference가 제공한 경우에만 연다.

## Mock Exam 복습 경로

사용자가 제공한 KodeKloud Mock Exam 14개 문제를 작업별 정본으로 연결한다.
각 절의 이름·namespace·요구값은 해당 사례에만 적용한다. 공식 자료는 풀이 옆에 연결했으며,
아래 검색어로 예제 블록을 다시 찾을 수 있다. 명령은 랩에서 실행할 절차이고 이 문서 업데이트에서
클러스터 실행을 검증한 기록은 아니다.

| 제공 문제 | 풀이 위치 | 첫 관찰·도움말 | 공식 검색어 |
|---|---|---|---|
| 1 · 네트워크 파라미터 영구 설정 | [sysctl 저장과 적용](/cka-udemy/cluster-lifecycle/#네트워크-파라미터-영구-적용) | `sysctl net.ipv4.ip_forward net.bridge.bridge-nf-call-iptables` | `Container Runtimes ip_forward` |
| 2 · PV 목록을 읽는 계정과 Pod | [pvviewer](/cka-udemy/service-account/#pvviewer--클러스터-pv-목록-권한과-pod-연결) | `kubectl create clusterrole --help` | `RBAC ClusterRole ServiceAccount` |
| 3 · StorageClass 생성 | [rancher-sc](/cka-udemy/storage-class/#rancher-sc--provisioner와-지연-바인딩-지정) | `kubectl get sc` | `Storage Classes volumeBindingMode` |
| 4 · ConfigMap 환경 변수 | [app-config와 cm-webapp](/cka-udemy/configmap-secret/#app-config--기존-deployment에-두-환경-변수-연결) | `kubectl get deploy cm-webapp -n cm-namespace -o yaml` | `kubectl set env --from` |
| 5 · 기존 Pod 우선순위 | [PriorityClass 적용](/cka-udemy/priority-class/) | `kubectl get pod lp-pod -n low-priority -o yaml` | `Pod Priority Preemption` |
| 6 · TCP 80 ingress 허용 | [ingress-to-nptest](/cka-udemy/network-policy/#ingress-to-nptest--기존-deny를-두고-tcp-80-허용) | `kubectl get pod np-test-1 -n default --show-labels` | `Network Policies ingress from` |
| 7 · production taint | [dev-redis와 prod-redis](/cka-udemy/scheduling/#dev-redis와-prod-redis--production-taint-통과) | `kubectl describe node node01` | `Taints Tolerations Dedicated Nodes` |
| 8 · PV/PVC 바인딩 복구 | [app-pvc](/cka-udemy/storage/#app-pvc--pv를-유지하고-accessmodes-불일치-수정) | `kubectl describe pvc app-pvc -n storage-ns` | `Persistent Volumes Binding` |
| 9 · kubeconfig 포트 | [super.kubeconfig](/cka-udemy/security/#superkubeconfig--mock-exam의-잘못된-포트) | `kubectl --kubeconfig=/root/CKA/super.kubeconfig cluster-info` | `Troubleshooting kubectl` |
| 10 · scale 미반영 복구 | [nginx-deploy](/cka-udemy/control-plane-failure/#nginx-deploy--scale-후-실행-파일-오타-복구) | `kubectl get deploy,rs,pods -n default` | `Create static Pods` |
| 11 · 사용자 정의 메트릭 HPA | [api-hpa](/cka-udemy/autoscaling/#api-hpa--pod별-requests_per_second-평균-목표) | `kubectl get deploy api-deployment -n api` | `HPA walkthrough custom metrics` |
| 12 · HTTPRoute 트래픽 분배 | [web-route](/cka-udemy/gateway/#web-route--두-service로-80-대-20-분배) | `kubectl get httproute web-route -n default -o yaml` | `HTTP traffic splitting` |
| 13 · 로컬 Helm chart로 교체 | [새 release 설치 후 이전 release 제거](/cka-udemy/helm-release-replacement/) | `helm list -A` | `helm lint install uninstall` |
| 14 · 클러스터 Pod CIDR 제출 | [podSubnet 추출](/cka-udemy/network-environment/#kubeadm의-pod-서브넷을-파일로-제출) | `kubectl get cm kubeadm-config -n kube-system -o yaml` | `kubeadm ClusterConfiguration podSubnet` |

## killer.sh 복습 경로

사용자가 제공한 [killer.sh](https://killer.sh) CKA Simulator A Kubernetes 1.35 자료에서
재사용할 작업을 추렸다. 아래 링크는 문제 순서가 아닌 **작업별 정본**이다. 이미 설명한
스토리지·kubeconfig는 기존 절에 연결하고, 빠진 절차와 해설의 주의점만 보강했다.
명령은 지정 랩에서 실행할 예이며 이 업데이트에서 실제 클러스터 실행을 검증한 것은 아니다.
제품 문서는 학습용으로 구분하고 시험의 버전·허용 자료는 각 문제 지시와 [시험 기준](/cka/exam/)을 따른다.

| 작업 | 복습 위치 | 첫 관찰·검색어 |
|---|---|---|
| SSH 인스턴스와 제출 호스트 | [작업 위치 확인](/cka-udemy/basics/#ssh-작업-위치와-제출-위치-확인) | `hostname`, `whoami`, `config current-context` |
| kubeconfig 이름·인증서 추출 | [지정 파일 읽기](/cka-udemy/jsonpath/#지정-kubeconfig에서-이름과-인증서-꺼내기) | `config get-contexts -o name`, `config view --raw` |
| Helm·CRD·cert-manager | [설치와 ClusterIssuer](/cka-udemy/cert-manager/) | `helm repo list`, `explain clusterissuer.spec.selfSigned` |
| StatefulSet 축소 | [owner 확인 후 scale](/cka-udemy/statefulset-scale/) | `ownerReferences`, `Scale a StatefulSet` |
| 퇴거 가능성이 큰 Pod | [BestEffort 후보와 판정 한계](/cka-udemy/resource-limits/#메모리-압박-때-퇴거-후보-찾기) | `status.qosClass`, `Node-pressure Eviction` |
| 환경별 HPA 전환 | [Kustomize HPA](/cka-udemy/kustomize-hpa/) | `kubectl kustomize`, `Horizontal Pod Autoscaling` |
| PV·PVC·workload 연결 | [정적 PV/PVC와 mount 검증](/cka-udemy/storage/) | `describe pvc`, `Persistent Volumes Binding` |
| 메트릭 조회 스크립트 | [컨테이너별 top](/cka-udemy/troubleshooting/#메트릭-조회-명령을-스크립트로-제출) | `kubectl top pod --help` |
| worker 버전 변경·조인 | [미조인 여부부터 구분](/cka-udemy/cluster-upgrade/#신규-worker의-패키지-변경과-조인-구분) | `kubelet --version`, `kubeadm join` |
| Pod에서 API 호출 | [CA·token과 응답 저장](/cka-udemy/pod-api-access/) | `Accessing API from a Pod`, `auth can-i list` |
| Secret·ConfigMap create 권한 | [허용·거부 검증](/cka-udemy/rbac/#생성-권한만-요구한-경우) | `create role --help`, `RBAC Role examples` |
| 모든 노드에 DaemonSet | [control-plane toleration](/cka-udemy/daemonset-static-pod/#control-plane에도-실행해야-할-때) | `describe node`, `DaemonSet tolerations` |
| 앱을 노드당 하나로 배치 | [필수 anti-affinity](/cka-udemy/scheduling/#같은-앱을-노드당-하나로-제한) | `Inter-pod anti-affinity`, `describe pod` |
| HTTPRoute 헤더 분기 | [match 조건과 우선순위](/cka-udemy/httproute-matching/) | `HTTP routing`, `HTTPRouteRule matches` |
| 인증서 만료일과 갱신 명령 | [조회와 실행 구분](/cka-udemy/security/#만료일-제출과-갱신-명령-기록) | `openssl x509 -enddate`, `kubeadm certs` |
| NetworkPolicy 목적지·포트 | [짝별 egress 규칙](/cka-udemy/network-policy/#목적지와-포트를-짝으로-제한) | `get pods -L app`, `NetworkPolicyEgressRule` |
| CoreDNS 추가 도메인 | [zone 추가와 복구](/cka-udemy/coredns-domain/) | `get cm coredns -o yaml`, `Customizing DNS Service` |
| Pod의 런타임 정보·로그 | [sandbox와 container 추적](/cka-udemy/troubleshooting/#pod에서-런타임-컨테이너까지-추적) | `get pod -o wide`, `crictl ps --pod` |

## 기초와 Workloads

| 작업·실습 | 첫 명령 | 공식 문서 검색어 |
|---|---|---|
| [kubectl 기본 조작](/cka-udemy/basics/) | `kubectl create --help` | `kubectl Cheat Sheet` |
| [JSONPath 추출·정렬](/cka-udemy/jsonpath/) | `kubectl get <resource> -o json` | `JSONPath Support` |
| [kubeconfig 원문 추출](/cka-udemy/jsonpath/#지정-kubeconfig에서-이름과-인증서-꺼내기) | `kubectl config view --raw` | `kubectl config view` |
| [Pod 생성·상태](/cka-udemy/pods/) | `kubectl get pods -o wide` | `Pod Lifecycle` |
| [Deployment rollout·rollback](/cka-udemy/workloads/) | `kubectl rollout status deploy/<name>` | `Deployments rolling back` |
| [Job 완료·실패](/cka-udemy/jobs/) | `kubectl get jobs` | `Jobs terminal conditions` |
| [command·args](/cka-udemy/pod-config/) | `kubectl get pod <name> -o yaml` | `Define Command and Arguments` |
| [ConfigMap·Secret 주입](/cka-udemy/configmap-secret/) | `kubectl create configmap --help` | `Configure Pod ConfigMap` |
| [init·sidecar](/cka-udemy/init-sidecar/) | `kubectl logs <pod> -c <container>` | `Sidecar Containers` |
| [securityContext](/cka-udemy/security-context/) | `kubectl exec <pod> -- id` | `Security Context` |
| [Pod 배치](/cka-udemy/scheduling/) | `kubectl describe pod <name>` | `Assign Pods to Nodes` |
| [resources·quota](/cka-udemy/resource-limits/) | `kubectl describe pod <name>` | `Resource Management for Pods` |
| [DaemonSet·static Pod](/cka-udemy/daemonset-static-pod/) | `kubectl get daemonsets -A` | `DaemonSet rolling update` |
| [HPA](/cka-udemy/autoscaling/) | `kubectl get hpa` | `Horizontal Pod Autoscaling` |
| [VPA 보충](/cka-udemy/vpa/) | `kubectl api-resources` | 설치된 CRD는 `kubectl explain vpa.spec` 우선 |

## Networking과 Storage

| 작업·실습 | 첫 명령 | 공식 문서 검색어 |
|---|---|---|
| [Service·EndpointSlice](/cka-udemy/services-dns/) | `kubectl get svc,endpointslices` | `Service EndpointSlice` |
| [DNS·CoreDNS](/cka-udemy/dns/) | `kubectl get pods -n kube-system -l k8s-app=kube-dns` | `Debugging DNS Resolution` |
| [네트워크 환경·CNI](/cka-udemy/network-environment/) | `kubectl get nodes -o wide` | `Container Runtimes CNI` |
| [Ingress·TLS](/cka-udemy/ingress-netpol/) | `kubectl get ingress,ingressclass` | `Ingress TLS` |
| [Gateway·HTTPRoute](/cka-udemy/gateway/) | `kubectl api-resources` | `Gateway API HTTPRoute` |
| [NetworkPolicy](/cka-udemy/network-policy/) | `kubectl get networkpolicy -A` | `Network Policies` |
| [볼륨·정적 PV/PVC](/cka-udemy/storage/) | `kubectl get pv,pvc` | `Persistent Volumes` |
| [StorageClass·동적 PV](/cka-udemy/storage-class/) | `kubectl get storageclass` | `Storage Classes WaitForFirstConsumer` |

## 접근 제어와 클러스터 운영

| 작업·실습 | 첫 명령 | 공식 문서 검색어 |
|---|---|---|
| [TLS·CSR·kubeconfig](/cka-udemy/security/) | `kubectl auth whoami` | `Issue Certificate Kubernetes API Client` |
| [RBAC](/cka-udemy/rbac/) | `kubectl auth can-i <verb> <resource>` | `Using RBAC Authorization` |
| [ServiceAccount·토큰](/cka-udemy/service-account/) | `kubectl get pod <name> -o yaml` | `Configure Service Accounts for Pods` |
| [imagePullSecrets](/cka-udemy/image-pull-secrets/) | `kubectl describe pod <name>` · `kubectl create secret -h` | `Pull an Image from a Private Registry` |
| [Admission](/cka-udemy/admission/) | `kubectl get --raw='/readyz?verbose'` | `Admission Controllers` |
| [설치된 CRD로 CR 생성](/cka-udemy/custom-resource/) | `kubectl api-resources --api-group=<group>` | `CustomResourceDefinition create custom objects` |
| [kubeadm 설치·CNI](/cka-udemy/cluster-lifecycle/) | `kubeadm init --help` | `Creating cluster kubeadm` |
| [drain·업그레이드](/cka-udemy/cluster-upgrade/) | `kubectl get nodes` | `Upgrading kubeadm clusters` |
| [etcd 백업·복구](/cka-udemy/etcd-backup-restore/) | `etcdctl version` | `Operating etcd clusters Kubernetes` |
| [Helm release](/cka-udemy/helm/) | `helm list -A` | `helm upgrade version` |
| [Kustomize 적용](/cka-udemy/kustomize/) | `kubectl kustomize <dir>` | `Declarative Management Kustomize` |
| [Kustomize 변환](/cka-udemy/kustomize-transformers/) | `kubectl kustomize <root>` | `Kustomize images labels namespace` |
| [Kustomize patch](/cka-udemy/kustomize-patches/) | `kubectl kustomize <overlay>` | `Kustomize patches` |
| [Kustomize Components 보충](/cka-udemy/kustomize-components/) | `kustomize build <overlay>` | 문제의 Quick Reference 또는 로컬 `kustomize --help` |

## Troubleshooting

| 작업·실습 | 첫 명령 | 공식 문서 검색어 |
|---|---|---|
| [metrics·앱 로그](/cka-udemy/troubleshooting/) | `kubectl get pods -A -o wide` | `Debug Pods` |
| [컨트롤 플레인 복구](/cka-udemy/control-plane-failure/) | `kubectl get pods -n kube-system` | `Troubleshooting Clusters crictl` |
| [워커 노드 복구](/cka-udemy/worker-failure/) | `kubectl get nodes` | `Troubleshooting Clusters worker nodes` |
| [Service·네트워크 장애](/cka-udemy/network-failure/) | `kubectl get svc,endpointslices` | `Debug Services` |

## 색인을 쓰고 끝내는 조건

1. 첫 명령으로 현재 상태를 저장하고, 증상만으로 원인을 단정하지 않는다.
2. 공식 예제의 API 버전·kind·필드 위치를 확인해 문제의 이름·namespace·값만 바꾼다.
3. 생성·수정된 오브젝트와 실제 동작을 나눠 확인한다. 장애 복구는 처음 실패한 요청까지 반복한다.
4. 기대한 허용뿐 아니라 거부·비변경·다른 namespace처럼 범위 밖 조건도 필요한 만큼 확인한다.

<LinkCard
  title="CKA 시험 전략과 치트시트"
  description="시간 배분, 문서 검색, 제출 전 검증 루프를 개념 덱에서 정리한다."
  href="/cka/exam-strategy/"
/>
