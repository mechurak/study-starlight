---
title: "10. 모니터링과 트러블슈팅"
description: "metrics-server 설치와 kubectl top, 컨테이너 로그, kube-proxy·CNI 네트워크 장애 진단"
---

import { LinkCard } from '@astrojs/starlight/components';

> 짝이 되는 개념 장: [cka 8. 오토스케일링](/cka/08-autoscaling/)(메트릭 파이프라인) · [cka 18. 트러블슈팅](/cka/18-troubleshooting/)

원본 레포의 트러블슈팅 실습 중 Control Plane Failure와 Worker Node Failure는
요약이 비어 있어 뺐다 — 그 두 유형의 진단 순서는 [cka 18장](/cka/18-troubleshooting/)이
컨트롤 플레인 층·노드 층 절에서 다룬다. 여기 남긴 것은 셋이다.

## metrics-server — Practice Test: Monitor Cluster Components

원본 랩: [practice-test-monitor-cluster-components](https://kodekloud.com/topic/practice-test-monitor-cluster-components/)

`kubectl top`은 **metrics-server가 있어야** 동작한다 — 기본 설치가 아니다.

```bash
# "Let us deploy metrics-server to monitor the PODs and Nodes. Pull the git repository for the deployment files."
git clone https://github.com/kodekloudhub/kubernetes-metrics-server.git
# "Deploy the metrics-server by creating all the components downloaded."
kubectl create -f kubernetes-metrics-server/
# "Identify the node that consumes the most CPU(cores)."
kubectl top node                  # CPU(cores) / MEMORY(bytes) 열
# "Identify the POD that consumes the most Memory(bytes)."
kubectl top pod
```

- 설치 직후에는 수집이 안 돼 있다 — **몇 분 기다렸다가** top이 값을 낼 때까지 재시도
  — *It takes a few minutes for the metrics server to start gathering data.*
- "가장 CPU를 많이 쓰는 노드/Pod" 유형은 전부 top 출력의 열 읽기다
- 정렬까지 시키면 `kubectl top pod --sort-by=memory`

메트릭이 어디서 와서 어디로 가는지(kubelet → metrics-server → HPA)는
[cka 8장 메트릭 파이프라인](/cka/08-autoscaling/)에 있다.

## 애플리케이션 로그 — Practice Test: Managing Application Logs

원본 랩: [practice-test-managing-application-logs](https://kodekloud.com/topic/practice-test-managing-application-logs/)

```bash
# "Inspect the logs of the POD"
kubectl logs webapp-1                  # 단일 컨테이너면 이걸로 끝
# "Inspect the logs of the webapp in the POD"
kubectl logs webapp-2 -c simple-webapp # 멀티 컨테이너면 -c 필수
kubectl logs webapp-1 -f               # 스트리밍
kubectl logs webapp-1 --previous       # 재시작 전 로그 — CrashLoopBackOff 진단의 핵심
```

:::tip[로그에서 답을 베끼는 유형]
"USER5의 로그인 실패 원인은?" 같은 문제는 로그 본문에서 답을 읽어 옮기는 것이다.
명령보다 **로그를 끝까지 읽는 침착함**이 채점 대상이다.
:::

## 네트워크 장애 — Practice Test: Troubleshoot Network

원본 랩: [practice-test-troubleshoot-network](https://kodekloud.com/topic/practice-test-troubleshoot-network/)

두 시나리오가 네트워크 층 진단의 대표 패턴이다.

**시나리오 1 — CNI가 없다.** — *Troubleshooting Test 1* — Pod이 `ContainerCreating`에 멈춰 있고, describe의
Events에 `unable to allocate IP address` — CNI(Container Network Interface) 플러그인이
설치되지 않았다는 신호다. 문제에서 주는 네트워크 플러그인 매니페스트를 apply하면 풀린다.
(원본 랩은 weave를 설치하지만 weave는 단종됐다 — **진단 패턴만** 가져간다.
`/etc/cni/net.d`가 비어 있는지 확인하는 방법은 [cka 12장](/cka/12-networkpolicy/).)

**시나리오 2 — kube-proxy가 CrashLoopBackOff.** — *Troubleshooting Test 2* — Service 접속이 안 될 때의 진단 사슬 —

```bash
kubectl get pods -n kube-system                      # kube-proxy가 크래시 중
# "Check the logs of the kube-proxy pod"
kubectl logs -n kube-system <kube-proxy-pod>         # "config file not found"
# "Inspect the pod template spec in the kube-proxy daemonset."
kubectl get ds -n kube-system kube-proxy -o yaml     # 볼륨: ConfigMap이 /var/lib/kube-proxy에 마운트
# "Inspect the config map"
kubectl describe cm -n kube-system kube-proxy        # 실제 파일명은 config.conf
# "Fix the command line arguments to kube-proxy"
kubectl edit ds -n kube-system kube-proxy            # --config=/var/lib/kube-proxy/config.conf 로 수정
```

:::caution[함정 — 로그 → 마운트 → ConfigMap 순서]
크래시하는 시스템 Pod은 **로그가 가리키는 파일**과 **실제 마운트된 파일**을
대조하는 것이 정석이다. DaemonSet의 커맨드라인 인자 오타(존재하지 않는 파일명)가
정답인 경우가 많다 — 로그를 안 보고 재시작만 반복하면 영원히 못 찾는다.
:::

## 이 장에서 남길 것

- `kubectl top`은 metrics-server 설치가 선행 조건, 값이 뜰 때까지 시간이 걸린다
- 로그는 `-c`(컨테이너 지정)와 `--previous`(재시작 전) 두 옵션이 점수를 가른다
- `ContainerCreating` + IP 할당 실패 = CNI 문제
- 시스템 Pod 크래시 = 로그 → 마운트 → ConfigMap 파일명 대조

<LinkCard
  title="다시 개요로"
  description="장별 실습 목록과 제외 목록 — 시험 직전에는 함정 aside만 훑는다."
  href="/cka-udemy/"
/>
