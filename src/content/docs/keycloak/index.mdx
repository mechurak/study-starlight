---
title: Keycloak 학습 지도
description: 인증 중앙화에서 로컬 SSO·API 인가·외부 디렉터리·운영까지 이어지는 학습 경로
reviewedAt: 2026-09-15
---

import { LinkCard, LinkButton } from '@astrojs/starlight/components';
import DeckMap from '../../../components/docs/DeckMap.astro';

> 앱은 디렉터리 password를 받지 않고, Keycloak이 증명한 신원을 검증해 자기 권한을 결정한다.

이 덱은 디렉터리·IdP 제품·로그인 프로토콜의 종류를 먼저 구분하고 Keycloak의 자리와 OIDC를 이해한 뒤,
Docker Compose에서 로컬 사용자 SSO와 API 인가를 재현한다. 이어서 Samba AD 호환 디렉터리를 LDAPS
Federation으로 연결하고 group이 token claim과 API 역할이 되는 두 매핑을 추적한다. 마지막에는
brokering·서비스 계정과 배포·관찰·복구 경계를 다룬다.

실습 기준은 Keycloak 26.7.3, PostgreSQL 18.6, Samba 4.19.5와 Node 24다. macOS/Colima 비브라우저
기본 흐름과 macOS의 대표 Chrome 로그인을 검증했지만 네이티브 Ubuntu, Microsoft AD DS, 운영 HA는 미검증이다.
[마무리](/keycloak/wrapup/)에서 통과·참조·보류 범위를 구분한다.

<LinkButton href="/keycloak/authentication-landscape/" icon="right-arrow">큰 그림부터 읽기</LinkButton>

<LinkCard
  title="Compose guided 실습 시작"
  description="기반 환경부터 Client·SSO·API·LDAP·group 권한까지 하나씩 설정하고 결과를 확인한다."
  href="/keycloak/lab-setup/"
/>

## 구성

<DeckMap deck="keycloak" />

## 전체를 관통하는 두 문장

**매핑은 두 번 일어난다.** 디렉터리의 group이 Keycloak 모델로 들어오는
[LDAP 매핑](/keycloak/directory-group-mapping/)과 Keycloak의 값이 token claim으로 실리는
[protocol 매핑](/keycloak/scopes-and-mappers/)은
**별개의 설정**이다. "앱에 그룹이 안 보인다"는 문제의 진단은 언제나
이 둘 중 어디서 끊겼는지 가르는 데서 시작한다.

**신뢰는 서명으로 흐른다.** 앱도, 쿠버네티스 API 서버도 Keycloak에 비밀번호를 물어보지 않는다.
Keycloak이 개인키로 서명한 토큰을, 각자가 공개키(JWKS)로 검증할 뿐이다.
그래서 인증은 한 곳에 모이고 검증은 각 소비자에서 일어난다. 최종 인가는 앱이나 Kubernetes RBAC의 몫이다.

## 문제·작업으로 바로 찾기

처음부터 읽지 않고 **지금 겪는 증상이나 할 일**로 들어가려면 이 표에서 시작한다.

| 찾는 것 | 먼저 볼 장 | 바로 확인할 것 |
|---|---|---|
| 실습을 처음부터 재현 | [실습 환경](/keycloak/lab-setup/) | runtime·자원·DNS/CA·secret 준비 |
| 로컬 로그인·앱 두 개 SSO | [Client 로그인 실습](/keycloak/client-login-lab/), [SSO·API 실습](/keycloak/sso-api-lab/) | client별 redirect/secret과 realm session |
| API token 401·403·200 | [Token 검증](/keycloak/token-validation/) | access token의 서명·iss·aud·exp와 role |
| AD 로그인이 안 됨 · LDAPS 오류 | [외부 계정 로그인 실습](/keycloak/directory-login-lab/) | CA, bind, READ_ONLY, import/sync/cache |
| group이 token/API 권한에 없음 | [외부 Group 권한 실습](/keycloak/directory-permissions-lab/) | LDAP mapper와 protocol mapper를 따로 확인 |
| 변경·disable·장애가 언제 반영됨 | [디렉터리 변경과 장애](/keycloak/directory-changes/) | 새 로그인/refresh/기존 JWT/app session |
| 다른 IdP·SAML·서비스 인증 | [Identity Brokering](/keycloak/identity-brokering/), [SAML](/keycloak/saml/), [Service Accounts](/keycloak/service-accounts/) | redirect trust와 machine identity를 구분 |
| Operator · DB · HA · 관찰 | [배포 설계](/keycloak/deployment/), [저장소와 가용성](/keycloak/storage-and-availability/), [인증 흐름 관찰](/keycloak/observability/) | 실습과 운영 보장을 분리 |
| 백업 · upgrade · key 교체 | [백업과 업그레이드](/keycloak/backup-and-upgrade/), [관리 권한과 서명 키](/keycloak/administration-and-keys/) | 격리 복원과 키 겹침 기간 |
| 오류 문구로 원인 찾기 | [Keycloak 문제 진단](/keycloak/troubleshooting/) | 마지막 성공 경계부터 확인 |
| 약어·용어만 빠르게 찾기 | [Keycloak 용어 사전](/keycloak/glossary/) | 역할별 뜻과 상세 페이지 링크 |

<LinkCard
  title="인증 생태계의 큰 그림"
  description="AD·LDAP·IdP·OAuth·OIDC·SAML·PKCE·SSO가 각각 무엇이고 어디에서 이어지는지 먼저 구분한다."
  href="/keycloak/authentication-landscape/"
/>
