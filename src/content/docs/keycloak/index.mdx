---
title: 개요
description: Keycloak — 사내 AD 인증을 SSO로, 온프렘 쿠버네티스 관리자의 관점에서
reviewedAt: 2026-08-12
---

import { LinkCard, LinkButton } from '@astrojs/starlight/components';
import DeckMap from '../../../components/docs/DeckMap.astro';

> 앱은 AD를 모르고, AD는 앱을 모른다 — 가운데서 **통역**하는 것이 Keycloak이다

사내에 AD(Active Directory)가 있고, 그 앞에 Keycloak을 세워 SSO(Single Sign-On)를
운영하는 환경을 전제로 한다. "로그인이 되게 하는 법"이 아니라
**관리자로서 알아야 하는 것** — 구조, AD 연동의 급소, 쿠버네티스 API 서버 연동,
온프렘 배포와 운영 — 을 다룬다.

쿠버네티스 기초는 [CKA 덱](/cka/) 수준을 전제하고, 여기서 다시 설명하지 않는다.

**2026년 8월 기준**으로 쓰였다 — Keycloak 26.7 · Kubernetes 1.35 · oauth2-proxy v7.

<LinkButton href="/keycloak/00-intro/" icon="right-arrow">첫 장부터 읽기</LinkButton>

## 구성

<DeckMap deck="keycloak" />

## 전체를 관통하는 두 문장

**매핑은 두 번 일어난다.** AD의 속성·그룹이 Keycloak으로 들어오는 매핑(경계 ①, [4장](/keycloak/04-ad-federation/))과,
Keycloak의 사용자 모델이 토큰의 claim으로 실리는 매핑(경계 ②, [3장](/keycloak/03-structure/))은
**별개의 설정**이다. "앱에 그룹이 안 보인다"는 문제의 진단은 언제나
이 둘 중 어디서 끊겼는지 가르는 데서 시작한다.

**신뢰는 서명으로 흐른다.** 앱도, 쿠버네티스 API 서버도 Keycloak에 비밀번호를 물어보지 않는다.
Keycloak이 개인키로 서명한 토큰을, 각자가 공개키(JWKS)로 검증할 뿐이다.
그래서 인증은 한 곳에 모이고, 검증은 어디서나 일어난다.

## 문제·작업으로 바로 찾기

처음부터 읽지 않고 **지금 겪는 증상이나 할 일**로 들어가려면 이 표에서 시작한다.

| 찾는 것 | 먼저 볼 장 | 바로 확인할 것 |
|---|---|---|
| AD 로그인이 안 됨 · 계정 잠금 · LDAPS 오류 | [4. AD 연동](/keycloak/04-ad-federation/) | bind 계정, 사내 CA, MSAD 계정 상태 매퍼 |
| 그룹·이메일·부서 claim이 토큰에 없음 | [3. 구조](/keycloak/03-structure/) → [4. AD 연동](/keycloak/04-ad-federation/) | Evaluate로 경계 ②를 먼저 가른 뒤 LDAP 매퍼 확인 |
| 자꾸 로그아웃됨 · 퇴사자 토큰 회수 | [5. 세션과 토큰](/keycloak/05-sessions/) | access token 수명과 SSO/offline session을 구분 |
| kubectl 401 · 403 · OIDC 설정 | [6. k8s OIDC](/keycloak/06-k8s-oidc/) | 401은 인증, 403은 RBAC과 그룹 prefix |
| 앱 SSO · oauth2-proxy · 사용자 헤더 | [7. 앱 연동](/keycloak/07-apps/) | OIDC 네이티브인지, 프록시가 필요한지부터 결정 |
| Operator · PostgreSQL · HA · hostname | [8. 배포](/keycloak/08-deploy/) | `v2beta1`, DB, issuer, `jdbc-ping` |
| 백업 · 업그레이드 · 키 교체 · 감사 | [9. 운영](/keycloak/09-ops/) | DB 복원, 호환성 검사, 키 겹침 구간 |
| 오류 문구로 원인 찾기 | [10. 트러블슈팅](/keycloak/10-troubleshooting/) | discovery → Evaluate → 이벤트 → 토큰 순서 |
| 약어·용어만 빠르게 찾기 | [11. 용어 사전](/keycloak/11-glossary/) | 영문 풀네임과 상세 장 링크 |

<LinkCard
  title="0. 시작하기 전에"
  description="이 덱의 대상과 범위, 미리 잡아두면 좋은 멘탈 모델 세 가지."
  href="/keycloak/00-intro/"
/>
