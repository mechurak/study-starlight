---
title: "OAuth 2.0과 OpenID Connect 로그인"
description: "Authorization Code와 PKCE로 로그인하고 access·ID·refresh token의 역할을 구분하는 흐름"
aliases: [OAuth 2.0, OIDC, Authorization Code, PKCE, ID token, access token, refresh token]
reviewedAt: 2026-09-14
deckGroup: login
sidebar:
  order: 1030
---

import Thesis from '../../../components/docs/Thesis.astro';
import TermIntro from '../../../components/docs/TermIntro.astro';

<Thesis>OIDC 로그인은 OAuth 2.0의 Authorization Code 흐름에 신원 확인을 더하며, 앱은 짧게 노출된 code를 검증된 token으로 교환한다.</Thesis>

사용자 비밀번호를 앱마다 받지 않으려면 브라우저를 Keycloak으로 보내 인증하고, 앱에는 그 결과만
돌려줘야 한다. 이 덱은 검증된 OIDC 라이브러리로 state·nonce·redirect와 Code+PKCE를 처리한다.

<TermIntro
  terms={[
    ['OAuth 2.0', '', '사용자나 서비스가 자격 증명을 직접 건네지 않고 제한된 접근 권한을 위임하는 프레임워크.'],
    ['OIDC', 'OpenID Connect', 'OAuth 2.0 위에 사용자 인증 결과와 ID token을 정의한 프로토콜.'],
    ['PKCE', 'Proof Key for Code Exchange', 'authorization code를 요청한 client만 교환할 수 있게 verifier와 challenge를 묶는 보호 장치.'],
    ['nonce', '', '로그인 요청과 ID token을 묶어 재사용·혼동을 막는 무작위 값. state는 요청과 callback 자체를 묶는다.'],
  ]}
/>

```d2 layout=elk pad=40 title="브라우저가 Keycloak에서 인증하고 앱이 code를 token으로 교환하는 Authorization Code와 PKCE 흐름"
direction: down

classes: {
  key: { style: { fill: "#dbeafe"; stroke: "#2563eb"; font-color: "#1e3a8a" } }
  ok: { style: { fill: "#dcfce7"; stroke: "#16a34a"; font-color: "#14532d" } }
  mute: { style: { fill: "#f1f5f9"; stroke: "#94a3b8"; font-color: "#334155" } }
}

browser: 브라우저 { class: mute }
app: 앱 { class: ok }
kc: Keycloak { class: key }

browser -> app: ① 로그인 시작
app -> browser: ② authorization URL\nstate · nonce · PKCE challenge
browser -> kc: ③ 인증 요청
kc -> browser: ④ 로그인 · 동의
browser -> app: ⑤ callback\ncode · state
app -> kc: ⑥ code + PKCE verifier
kc -> app: ⑦ ID · access · refresh token
app -> browser: ⑧ 앱 session
```

## 이 장에서 답할 질문

- OAuth 2.0과 OIDC는 무엇이 다른가?
- Authorization Code + PKCE에서 브라우저와 앱은 무엇을 주고받는가?
- ID·access·refresh token은 누가 어디에 쓰는가?

## OAuth 위에 인증을 얹는다

OAuth 2.0의 핵심 질문은 "이 client가 어떤 resource에 접근하도록 위임받았는가"다. OIDC는
authorization request에 `openid` scope를 넣고 ID token·UserInfo·표준 claim을 정의해 "누가
로그인했는가"를 추가한다. OAuth access token만 보고 앱 로그인 정보를 임의로 추측하면 안 된다.

## Code+PKCE의 보호 장치

[Keycloak의 OIDC endpoint 안내](https://www.keycloak.org/securing-apps/oidc-layers)는 discovery,
authorization, token, JWKS endpoint를 설명한다. 앱은 endpoint를 문자열로 흩어 적기보다 realm의
discovery 문서에서 읽는다.

| 값 | 막으려는 문제 | 확인 주체 |
|---|---|---|
| 정확한 redirect URI | 공격자 callback으로 code가 새는 문제 | Keycloak |
| `state` | 다른 브라우저 요청의 callback을 끼워 넣는 문제 | 앱 |
| PKCE `S256` | 탈취한 code를 다른 client가 교환하는 문제 | Keycloak |
| `nonce` | 다른 인증의 ID token 재사용·혼동 | 앱의 OIDC 라이브러리 |

public client는 secret을 안전하게 숨길 수 없으므로 PKCE가 특히 중요하다. confidential server-side
앱도 PKCE를 함께 쓰면 code 탈취 방어가 더 명확해진다. 이 실습의 앱 A/B는 confidential client지만
`S256`을 요구한다.

## 세 token의 역할

| token | 받는 사람과 목적 | 쓰면 안 되는 곳 |
|---|---|---|
| ID token | client가 로그인한 사용자와 인증 사건을 확인 | API bearer 권한 판단 |
| access token | API가 서명·issuer·audience·만료와 권한 claim을 검증 | 브라우저 프로필 표시의 유일한 근거 |
| refresh token | client가 새 token을 요청 | API 요청의 bearer credential |

JWT는 암호화된 비밀 상자가 아니라 서명된 claim 묶음인 경우가 많다. 브라우저 console이나 로그에
원문을 남기지 않고, access token은 의도한 API에만 보낸다.

## 실습에서 확인한 실패

앱 A 자동 검증은 올바른 local-user로 callback과 앱 session까지 성공했다. 오답 비밀번호는 callback에
도달하지 않았고, 변조한 state·nonce는 앱이 HTTP 400으로 거부했으며, 미등록 redirect URI는
Keycloak이 거부했다. password grant로 이 흐름을 대체하지 않았다.

## 요약

Authorization Code + PKCE를 앱 코드와 실제 callback으로 확인하려면
[Client를 연결하고 로그인 확인하기](/keycloak-lab/client-login-lab/)로 이어 간다.

- OAuth 2.0은 접근 위임, OIDC는 그 위에 사용자 인증을 정의한다.
- Authorization Code 흐름에서 code는 짧게 브라우저를 지나고, 앱이 PKCE verifier로 token을 교환한다.
- state·nonce·redirect URI·PKCE는 서로 다른 공격 경계를 막는다.
- 로그인에는 ID token, API에는 access token, 갱신에는 refresh token을 쓴다.
