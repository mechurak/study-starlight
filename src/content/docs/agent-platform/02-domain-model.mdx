---
title: "2. 제품 밖의 domain model"
description: Agent의 분류 축과 정체성·버전·trigger·권한·배포를 kagent CRD와 AWS ARN 밖에 정의한다
---

import { LinkCard } from '@astrojs/starlight/components';
import TermIntro from '../../../components/docs/TermIntro.astro';

> `Agent`는 서비스의 정체성이고 `Deployment`는 그 정체성을 실행한 한 복사본이다

<TermIntro
  terms={[
    ['aggregate', 'Domain Aggregate', '함께 일관성을 지켜야 하는 객체 묶음. 여기서는 Agent가 version·grant·publication의 기준점이다.'],
    ['immutable version', 'Immutable Version', '한번 승인·배포되면 내용을 바꾸지 않는 snapshot. 변경은 다음 version으로 만든다.'],
    ['providerRef', 'Provider Reference', 'kagent resource name이나 AgentCore ARN처럼 provider에서만 의미 있는 식별자다.'],
    ['desired state', 'Desired State', 'control plane이 기대하는 배포 상태. adapter가 provider의 실제 상태를 이 값에 맞춘다.'],
    ['trigger', 'Invocation Trigger', '사람의 요청·schedule·event처럼 Agent 실행을 시작시키는 계기와 실행 정책이다.'],
  ]}
/>

## 객체 관계

```d2 layout=elk pad=40 title="Agent와 Tool의 정체성에서 Version·Grant·Publication·Trigger를 거쳐 target별 Deployment와 providerRef로 이어지는 객체 관계"
direction: right

classes: {
  key: {
    style: {
      fill: "#dbeafe"
      stroke: "#2563eb"
      font-color: "#1e3a8a"
    }
  }
  zone: {
    style: {
      fill: "#ede9fe"
      stroke: "#7c3aed"
      font-color: "#4c1d95"
    }
  }
  ok: {
    style: {
      fill: "#dcfce7"
      stroke: "#16a34a"
      font-color: "#14532d"
    }
  }
}

agent: "Agent\n정체성 · owner" { class: key }
ver: "AgentVersion\nimmutable artifact" { class: key }
tool: "Tool\n정체성 · owner" { class: key }
toolver: "ToolVersion\nartifact · schema" { class: key }
grant: "Grant\nprincipal · role" { class: zone }
pub: "Publication\n공개 상태" { class: zone }
trg: "Trigger\nrequest · schedule · event" { class: zone }
binding: "ToolBinding\nallowlist · auth mode" { class: zone }
target: "DeploymentTarget\nzone · capabilities" { class: ok }
dep: "Deployment\nversion × target" { class: ok }
tdep: "ToolDeployment\n실행 또는 연결" { class: ok }
pref: "providerRef\nnamespace/name 또는 ARN" { class: ok }

agent -> ver
agent -> grant
agent -> pub
agent -> trg
ver -> dep
target -> dep
dep -> pref
tool -> toolver
toolver -> tdep
target -> tdep
tdep -> pref
ver -> binding
binding -> toolver
pub -> dep
trg -> pub
```

## Agent를 하나의 enum으로 나누지 않는다

`Declarative`·`BYO`나 `chatbot`·`batch agent`처럼 한 단어로 Agent 종류를 정하면 서로 다른 질문이 섞인다.
예를 들어 매일 보고서를 만드는 Agent는 코드형일 수도 구성형일 수도 있고, 매번 새 process에서 실행할 수도
상주 service를 `CronJob`이 호출할 수도 있다. 플랫폼은 최소한 다음 축을 독립적으로 기록해야 한다.

| 분류 축 | 답할 질문 | 대표 값 |
|---|---|---|
| 작성 방식 | 실행 logic을 누가 공급하나 | `CONFIGURED`, `CODE` |
| 활성화 방식 | 무엇이 실행을 시작하나 | `REQUEST`, `SCHEDULE`, `EVENT` |
| 상주 방식 | 요청이 없을 때 process가 남나 | `RESIDENT`, `SUSPENDABLE`, `EPHEMERAL` |
| 상태·복구 | 중단 뒤 어디서 이어가나 | `TURN`, `SESSION`, `DURABLE_WORKFLOW`, `WORKSPACE` |
| 격리 | 무엇과 failure·filesystem 경계를 공유하나 | `SHARED`, `POD`, `SANDBOX`, `MICROVM` |
| 행위 위험 | 어떤 권한과 data를 다루나 | `READ`, `WRITE`, `DESTRUCTIVE`, `CODE_EXEC` |

### 밖에서 부르는 이름을 축으로 번역한다

업계에서 흔히 쓰는 Agent 분류 이름은 대부분 위 축들의 특정 조합을 부르는 별칭이다. 이름은 검색과
소통에 유용하지만 배포 계약에 그대로 넣을 단위는 아니므로, 기록은 축 값으로 한다.

| 통용 이름 | 대체로 가리키는 것 | 이 덱의 축으로 번역하면 |
|---|---|---|
| chatbot · Q&A agent | 사람과 turn 단위로 대화한다 | 활성화 `REQUEST` · 상태 `SESSION` · 위험 `READ` 중심 |
| copilot · assistant | 사람이 매 단계 결과를 확인하고 승인한다 | `REQUEST` + human approval — 위험 축을 낮게 유지하는 정책 |
| autonomous agent | LLM이 도구와 순서를 스스로 정해 여러 step을 진행한다 | 특정 축 값이 아니라 행위 위험·격리 축의 상한 요구 |
| background · ambient agent | 사람이 자리에 없을 때 실행된다 | 활성화 `SCHEDULE`·`EVENT` + service principal |
| multi-agent | 여러 Agent가 협업한다 | 각 Agent를 독립 등록하고 orchestrator도 하나의 Agent로 취급 — 연결은 A2A·event |

또 하나 자주 만나는 구분은 workflow 대 agent다.
[Anthropic의 Building effective agents](https://www.anthropic.com/engineering/building-effective-agents)가
널리 퍼뜨린 이 구분은 실행 경로를 code가 미리 고정하는가(workflow), LLM이 매 step 스스로 정하는가(agent)를
나눈다. 이 구분은 위 여섯 축과 직교한다 — 경로가 고정된 workflow도 schedule로 활성화되는 ephemeral
process일 수도, 항상 상주하는 service일 수도 있다. 그래서 이 덱은 이것을 별도 enum으로 두지 않고
`AgentVersion`의 구현 설명과 risk 평가의 입력으로만 쓴다.

### 활성화와 상주를 분리한다

`SCHEDULE`은 언제 호출할지를 말하고 `EPHEMERAL`은 호출할 때 만든 process가 끝나면 종료되는지를 말한다.
둘을 합치면 다음 두 설계를 구분하지 못한다.

```d2 pad=40 title="같은 주기 trigger가 상주 Agent service를 A2A로 호출하는 구성과 실행 후 종료되는 Agent Job을 만드는 구성의 비교"
direction: right

classes: {
  key: {
    style: {
      fill: "#dbeafe"
      stroke: "#2563eb"
      font-color: "#1e3a8a"
    }
  }
  ok: {
    style: {
      fill: "#dcfce7"
      stroke: "#16a34a"
      font-color: "#14532d"
    }
  }
  warn: {
    style: {
      fill: "#fef3c7"
      stroke: "#d97706"
      font-color: "#78350f"
    }
  }
}

resident: "주기 호출 · 상주 runtime" {
  cron: "CronJob" { class: key }
  service: "Agent service\n계속 Ready" { class: ok }
  cron -> service: "A2A invoke"
}

ephemeral: "주기 호출 · 일회성 runtime" {
  cron: "CronJob" { class: key }
  job: "Agent Job\n실행 후 종료" { class: warn }
  cron -> job
}
```

첫 방식은 cold start가 적고 여러 trigger가 같은 endpoint를 공유하기 쉽다. 대신 idle replica 비용이 든다.
두 번째는 실행하지 않을 때 compute를 쓰지 않지만 시작 지연, image pull, 실행별 secret·trace 수집과 재시도 정책을
직접 다뤄야 한다. `SUSPENDABLE`은 둘 사이에 있다. 논리적 Agent와 state는 남기고 idle process만 snapshot하거나
scale-to-zero한 뒤 요청 때 복구한다.

### 상태 수명은 process 수명과 다르다

Pod가 재시작돼도 workflow state가 저장돼 있으면 긴 작업을 이어갈 수 있다. 반대로 Pod가 계속 떠 있어도 state를
memory에만 두면 재시작 한 번에 session이 사라진다.

| 상태 모델 | 보존해야 할 것 | 대표 용도 |
|---|---|---|
| `TURN` | 단일 요청의 입력·출력 | 번역·요약 같은 독립 호출 |
| `SESSION` | 대화 history·memory reference | chat·질의응답 |
| `DURABLE_WORKFLOW` | step·retry·timer·approval 대기 | 장애 조치·장기 업무 자동화 |
| `WORKSPACE` | filesystem·process·terminal context | coding Agent·조사 sandbox |

따라서 health check의 `Ready`만으로 복구 가능성을 판단하지 않는다. version contract에는 state backend,
checkpoint 단위, retry 시 side effect의 idempotency, session retention을 함께 둔다.

### 분류 조합으로 운영 정책을 고른다

| 예 | 작성 | 활성화 | 상주 | 상태 | 주요 policy |
|---|---|---|---|---|---|
| 사내 문서 질의 | 구성형 | 요청 | 상주 또는 suspend | session | read-only tool·session retention |
| 매일 비용 보고서 | 구성형 또는 코드형 | schedule | 상주 호출 또는 ephemeral | turn | timezone·중복 실행·catch-up |
| 장애 자동 조치 | 코드형 | event | 상주 | durable workflow | approval·idempotency·재시도 상한 |
| coding Agent | 코드형 | 요청 | suspend | workspace | 강한 sandbox·egress·filesystem quota |

### 제품의 분류는 축의 부분 답이다

제품 이름은 이 조합을 결정한 뒤 고른다. 세 제품이 자기 API에서 Agent를 구분하는 방식은 서로 다른
축의 부분 답이고, 어느 하나도 여섯 축 전체를 대신하지 않는다. 특히 행위 위험 축은 세 제품 모두
Agent 분류가 아니라 별도 policy 층(tool allowlist, Dapr access policy, Cedar)으로만 다룬다.

| 제품 | 제품이 구분하는 것 | 주로 답하는 축 | 답하지 않는 축 |
|---|---|---|---|
| kagent | `Agent.spec.type`의 `Declarative`·`BYO`, 별도 kind인 `SandboxAgent`·`AgentHarness` | 작성 방식 · 상주(suspend)·격리 · workspace 상태 | 활성화 — core spec에 schedule이 없다 |
| Dapr Agents | `DurableAgent` 클래스(동기 `Agent` 클래스는 deprecated), multi-agent orchestrator | 상태·복구(`DURABLE_WORKFLOW`) | 상주·격리 — 어느 클래스든 일반 Pod다 |
| AgentCore | Agent 타입 없음 — protocol contract(HTTP·MCP·A2A·AG-UI)와 session runtime | 격리(`MICROVM`) · managed 상주 | 작성 방식·활성화 |

각 제품이 왜 이렇게 답하는지는 [7장](/agent-platform/07-kagent/)·[8장](/agent-platform/08-dapr-agents/)·[9장](/agent-platform/09-agentcore/)에서
확인한다.

### Domain 객체에는 축별로 나눠 담는다

- `Agent`: 업무 정체성, owner, data classification과 기본 risk tier
- `AgentVersion`: 작성 방식, artifact, protocol, state·recovery contract
- `Trigger`: request·schedule·event source, 실행 principal, task template, timeout·retry·concurrency policy
- `DeploymentTarget`: 가능한 isolation·residency·network·state capability
- `Deployment`: 선택된 target에서 실제 적용한 replica·sandbox·provider reference

schedule 문자열을 image 환경 변수에 숨기거나 `Agent.type`에 `CRON_CODE_AGENT` 같은 조합값을 늘리지 않는다.
trigger를 별도 객체로 두면 같은 승인된 version을 사용자 요청과 야간 schedule에서 함께 호출하되 principal·quota·
timeout은 다르게 적용할 수 있다.

## Agent와 AgentVersion

`Agent`에는 바뀌어도 정체성이 유지되는 값을 둔다.

- 회사 내 고유 `agentId`
- 표시 이름, 설명, owner principal
- 업무 data와 tool action 기준의 기본 risk tier
- 업무 domain과 data classification
- 생성·폐기 상태

실행 결과를 바꿀 수 있는 값은 `AgentVersion`으로 내린다. 작성 방식, image digest, prompt bundle, model policy,
tool binding, runtime contract와 resource hint가 여기에 포함된다. `latest` tag나 mutable Git branch를 승인된
version의 근거로 쓰지 않는다.

## Trigger는 Agent process가 아니다

사람이 portal에서 누르는 invoke는 즉시 trigger이고, scheduler와 event bus는 비대화형 trigger다. trigger가
실행됐다고 새 Agent process를 반드시 만드는 것은 아니다. placement policy가 상주 endpoint 호출, sandbox 복원,
일회성 Job 생성 중 target이 지원하는 방식을 고른다.

```yaml
triggerId: trg_daily_finance
agentId: agt_finance_report
publication: production
type: SCHEDULE
schedule:
  cron: "0 8 * * 1-5"
  timezone: Asia/Seoul
principalRef: svc_finance_reporter
taskTemplate: "전 영업일 비용 보고서를 생성한다."
concurrencyPolicy: FORBID
timeout: 15m
retry:
  maxAttempts: 2
```

schedule에는 timezone, missed run 처리, 동시 실행 정책이 필요하다. event trigger에는 source, event schema,
deduplication key와 acknowledgment policy가 필요하다. 둘 다 호출 사용자의 권한을 빌리지 않고 명시적인 service
principal로 실행하며, 시작한 trigger ID를 trace와 audit event에 남긴다.

## Grant와 Publication

`Grant`는 `(agentId, principal, role)`의 관계다. role은 처음에는 세 개면 충분하다.

| role | 허용할 대표 action |
|---|---|
| `INVOKER` | Agent 검색·호출, 자신의 conversation 조회 |
| `EDITOR` | 새 version 작성·제출, metadata 변경 |
| `OWNER` | grant 변경, owner 이전, 폐기 요청 |

`Publication`은 배포와 다르다. runtime에 정상 배포됐어도 보안 승인과 smoke test가 끝나기 전에는 catalog에서
호출 가능 상태가 아니다. 반대로 새 배포가 실패해도 이전 published version은 계속 서비스할 수 있어야 한다.

## DeploymentTarget과 Deployment

`DeploymentTarget`은 단순 provider 이름이 아니라 placement policy가 판단할 capability 묶음이다.

```yaml
id: onprem-restricted-a
provider: kagent
zone: restricted
capabilities:
  protocols: [a2a, mcp]
  architectures: [amd64]
  internetEgress: false
  sessionIsolation: pod
  localModelAccess: true
```

`Deployment`는 특정 version을 특정 target에 실행한 결과다. 여기에서만 provider 정보를 가진다.

```yaml
deploymentId: dep_01J...
agentId: agt_finance_report
versionId: ver_7
targetId: onprem-restricted-a
desiredState: RUNNING
observedState: READY
providerRef:
  namespace: agent-finance
  name: finance-report-v7
endpointRef: ep_01J...
```

같은 version을 AgentCore에 배포하면 `providerRef`가 runtime ARN과 endpoint qualifier로 바뀔 뿐 `agentId`,
`versionId`, `Grant`는 그대로다.

## ToolBinding은 endpoint 목록이 아니다

tool을 URL만으로 연결하면 실행 권한의 의미가 사라진다. binding에는 다음을 함께 둔다.

- 논리적 `toolId`와 승인된 schema version
- read·write·destructive risk
- 필요한 OAuth scope 또는 workload permission
- user delegation, service identity 중 어느 auth mode인지
- 허용 data zone과 egress destination
- human approval이 필요한 action

실제 cluster Service URL이나 AgentCore Gateway target ARN은 environment별 `ToolDeployment.providerRef`에 둔다.
사용자가 만든 MCP는 `Tool`·`ToolVersion`·`ToolDeployment`로 먼저 등록·검증한 뒤 binding한다. 이 공급 lifecycle은
[사용자 MCP를 플랫폼에 올리기](/agent-platform/06-user-mcp/)에서 이어서 설계한다.

## 식별자와 표시값을 분리한다

이메일과 부서명은 바뀐다. ACL key는 IdP의 불변 `sub`·object ID를 사용하고 화면에 현재 email과 display name을
붙인다. Agent 이름도 rename할 수 있으므로 URL과 audit event에는 `agentId`를 쓴다.

모든 trace와 audit event에는 최소 다음 correlation field가 있어야 한다.

```text
agentId · versionId · deploymentId · targetId · triggerId · principalId · sessionId · traceId
```

## 지켜야 할 불변조건

1. published version은 수정하지 않는다.
2. 하나의 production publication은 하나의 active version을 가리킨다.
3. provider resource를 삭제해도 Agent와 audit 기록은 삭제하지 않는다.
4. owner가 사라지기 전에 group owner 또는 successor에게 이전한다.
5. deployment `READY`와 publication `AVAILABLE`을 같은 상태로 취급하지 않는다.
6. secret value를 version spec과 audit payload에 넣지 않는다.

## 2장 요약

- Agent와 Tool은 정체성, Version은 실행 가능한 불변 snapshot, Deployment는 target별 실행·연결 결과다.
- 작성·활성화·상주·상태·격리·위험을 독립 축으로 두고, schedule과 event는 `Trigger`로 표현한다.
- Grant와 Publication을 provider resource 밖에 두어 호출 권한과 공개 상태를 유지한다.
- provider-specific ID·URL·secret은 공통 객체의 중심으로 들어오지 않는다.

<LinkCard
  title="3. 등록부터 폐기까지"
  description="version이 어떤 검사를 거쳐 배포·공개되고, 실패하면 어느 상태로 돌아가는지 잇는다."
  href="/agent-platform/03-lifecycle/"
/>
