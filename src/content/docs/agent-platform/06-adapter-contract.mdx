---
title: "6. Adapter contract"
description: validate·deploy·status·stop과 capability profile로 runtime provider 경계를 고정하고 publication 전환·durability와 분리한다
---

import { LinkCard } from '@astrojs/starlight/components';
import TermIntro from '../../../components/docs/TermIntro.astro';
import Thesis from '../../../components/docs/Thesis.astro';

<Thesis>좋은 adapter는 차이를 지우지 않고 차이가 나타나는 한 곳을 만든다</Thesis>

<TermIntro
  terms={[
    ['port', 'Ports and Adapters', '핵심 domain이 외부 제품을 직접 호출하지 않고 정해진 interface를 통해 연결하는 구조다.'],
    ['capability profile', 'Capability Profile', 'target이 지원하는 protocol·architecture·network·session·storage 특성을 기계가 읽을 수 있게 표현한 값이다.'],
    ['idempotency', 'Idempotency', '같은 배포 요청을 여러 번 보내도 resource가 중복 생성되지 않고 같은 결과에 수렴하는 성질이다.'],
  ]}
/>

## Adapter의 자리

```d2 layout=elk pad=40 title="Platform Core가 RuntimeAdapter port로 세 어댑터를 부르고 각 어댑터의 제품 resource가 하나의 Normalized DeploymentStatus로 돌아오며, durability port는 현재 none이고 재개 시 후보만 점선으로 붙어 있는 구조"
direction: down

classes: {
  key: {
    style: {
      fill: "#dbeafe"
      stroke: "#2563eb"
      font-color: "#1e3a8a"
    }
  }
  zone: {
    style: {
      fill: "#ede9fe"
      stroke: "#7c3aed"
      font-color: "#4c1d95"
    }
  }
  ok: {
    style: {
      fill: "#dcfce7"
      stroke: "#16a34a"
      font-color: "#14532d"
    }
  }
}

core: "Platform Core\nAgent · Knowledge · Version · Grant" { class: key }
port: RuntimeAdapter port { class: zone }
dport: "Durability port\nExecutionProfile" { class: zone }

k0: "KubernetesAdapter\n기준선" { class: ok }
ka: KagentAdapter { class: ok }
aa: AgentCoreAdapter { class: ok }

k8s0: Deployment · Service · Job { class: ok }
k8s: Agent CR · Service · Secret { class: ok }
aws: "OCI/CodeZip · Runtime · Endpoint" { class: ok }

dnone: "none\n현재" { class: zone }
ddefer: "Temporal · Dapr Agents\n재개 시 후보" { class: zone }

normal: Normalized DeploymentStatus { class: zone }

core -> port
core -> dport
port -> k0
port -> ka
port -> aa
k0 -> k8s0
ka -> k8s
aa -> aws
dport -> dnone
dport -> ddefer: { style.stroke-dash: 3 }
k8s0 -> normal
k8s -> normal
aws -> normal
normal -> core
```

platform core는 Kubernetes client나 AWS SDK를 import하지 않는다. adapter가 provider credential, retry,
eventual consistency와 error message를 소유한다. 맨 Kubernetes·kagent·AgentCore는 workload를 만드는 같은
port의 구현이다. [보류한 durable execution 층](/agent-platform/12-durable-execution/)은 이 port의 세 번째
구현이 아니다. 별도 durability port가 `ExecutionProfile`을 검증하고, 선택한 RuntimeAdapter가 만든 workload
안의 worker와 조합한다.

## 최소 operation

| operation | 입력 | 출력·약속 |
|---|---|---|
| `validate` | version, target | 지원 여부와 고칠 수 있는 이유. 외부 변경 없음 |
| `plan` | version, target, current | 생성·변경·삭제될 논리 resource. 외부 변경 없음 |
| `deploy` | approved version, target, idempotency key | deployment와 providerRef |
| `getStatus` | deployment | normalized state, health, provider detail link |
| `stop` | deployment, drain policy | 새 invocation 차단과 session 처리 결과 |
| `setTraffic` | ready deployments, weights | provider가 지원할 때만 endpoint-version traffic 반영 |
| `delete` | deployment, retention policy | runtime resource 제거. domain record는 유지 |

실제 invocation은 별도 `RuntimeInvoker` port로 분리할 수 있다. 배포 credential과 호출 credential의 권한 범위가
다르고, streaming·session 처리는 control API보다 request path에 가깝기 때문이다.

`PublicationRouter.switchActiveDeployment(publicationId, deploymentId)`는 platform core의 operation이다.
adapter가 provider-native traffic split을 지원하면 `setTraffic`으로 구현을 보조할 수 있지만, 어느 Deployment가
회사 catalog의 active version인지 결정하지 않는다. 따라서 publication rollback과 provider resource repair를
같은 `rollback` method로 합치지 않는다.

## 공통 spec은 의도만 담는다

```yaml
runtime:
  protocol: a2a
  artifact:
    kind: OCI_IMAGE # CONFIG_BUNDLE | CODE_ZIP
    uri: registry.company/agents/report
    digest: sha256:...
    architecture: amd64
  configuration:
    promptBundleDigest: sha256:...
    knowledgeBindings:
      - knowledgeVersionId: knv_hr_policy_2026_08
        retrievalPolicyVersion: rpv_3
    policyBundleVersion: pbv_12
    effectiveConfigHash: sha256:...
  executionProfile:
    durability: none
  resources:
    cpu: "1"
    memory: 2Gi
  network:
    zone: restricted
    egressProfiles: [model-internal, finance-mcp]
  session:
    mode: stateful
    maxDuration: 30m
  secrets:
    - logicalRef: finance-read-token
```

여기에 Kubernetes `nodeSelector`나 AWS subnet ID를 넣지 않는다. target policy가 zone과 capability를 실제
namespace, subnet, security group, execution role로 번역한다. `CODE_ZIP`은 AgentCore 같은 일부 target만
지원하는 artifact extension이고, portable production 기본값은 immutable OCI digest다.

## Capability로 차이를 드러낸다

여러 provider의 모든 기능을 공통 boolean으로 만들면 어느 쪽도 제대로 쓰지 못한다. target은 다음처럼 자신을 설명한다.

```yaml
capabilities:
  protocols: [http, mcp, a2a]
  architectures: [arm64]
  isolation: session-microvm
  maxSessionDuration: 8h
  privateIngress: true
  privateEgress: true
  persistentFilesystem: supported
  trafficSplit: endpoint-version
```

placement policy는 version requirement와 capability를 비교한다. 필수 capability가 없으면 `validate` 단계에서
거부한다. 선택 기능이면 warning과 degradation을 명시하고 owner가 승인하게 한다.

## 상태와 오류를 정규화한다

공통 상태는 적게 둔다.

```text
PENDING → PROVISIONING → READY → DRAINING → STOPPED
                     ↘ DEGRADED
                     ↘ FAILED
어느 상태에서든 provider 조회 실패 → UNKNOWN
```

provider 원문 상태와 reason은 버리지 않는다. `FAILED`만 반환하면 운영자가 `ImagePullBackOff`인지 ECR ARM64
불일치인지 알 수 없다. normalized `reasonCode`와 redacted `providerMessage`, provider console/log link를 함께 준다.

## Idempotency와 reconciliation

`deploy` timeout 뒤 같은 요청을 재시도해도 resource가 하나만 생겨야 한다. provider resource에는
`deploymentId` label/tag를 붙이고, create 전에 그 key로 조회한다. adapter 호출은 request transaction 하나로
완료된다고 가정하지 않고 reconciler가 반복한다.

삭제도 같은 원칙을 쓴다. provider resource가 이미 없으면 성공으로 수렴하되, 다른 deployment가 소유한 shared
resource는 지우지 않는다.

## Secret과 identity mapping

logical secret은 adapter가 target별 reference로 바꾼다.

| 공통 의도 | 맨 Kubernetes target | kagent target | AgentCore target |
|---|---|---|---|
| runtime workload identity | Kubernetes ServiceAccount | Kubernetes ServiceAccount | AgentCore workload/execution role |
| secret reference | Secret/Vault injection | Secret/Vault injection | Secrets Manager·AgentCore Identity provider |
| private CA | Secret volume·image trust store | Secret volume·ModelConfig TLS | artifact trust store·private endpoint 설정 |
| egress policy | NetworkPolicy·proxy | NetworkPolicy·proxy | security group·VPC endpoint·route |

secret value를 adapter DB나 provider tag에 복제하지 않는다. target이 늘어나면 이 표에 column이
하나 추가될 뿐 공통 의도 행은 그대로다 — 그것이 logical reference를 쓰는 이유다.

## Contract test

모든 adapter는 같은 test suite를 통과해야 한다.

- 같은 idempotency key 두 번 배포
- 지원하지 않는 architecture와 protocol 거부
- create 성공 뒤 응답 유실을 재현하고 복구
- ready → invoke → stop → invoke 거부
- provider resource 수동 삭제 뒤 drift 감지
- log와 trace에 공통 correlation field 존재

platform integration suite는 별도로 새 Deployment를 만든 뒤 publication pointer를 전환·복귀하고, adapter의
`stop`·`delete`가 Publication을 암묵적으로 바꾸지 않는지 검사한다. durability를 재개하면 같은 RuntimeAdapter
suite를 재사용하고 별도 execution recovery suite를 추가한다.

## 6장 요약

- platform core와 provider SDK 사이에 배포와 호출 port를 둔다.
- publication pointer 전환은 platform core, provider traffic 조정은 adapter의 선택 capability다.
- 공통 spec에는 의도, target에는 실제 인프라 mapping을 둔다.
- capability negotiation으로 backend 고유 기능을 보존한다.
- adapter는 idempotency, eventual consistency, provider error 번역을 책임지고 durability는 별도 port로 조합한다.

<LinkCard
  title="7. 사용자 MCP를 플랫폼에 올리기"
  description="사용자가 만든 MCP를 독립된 Tool lifecycle로 등록·검증·배포·공개한다."
  href="/agent-platform/07-user-mcp/"
/>
